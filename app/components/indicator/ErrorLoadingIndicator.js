import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

function ErrorLoadingIndicator(props) {
	return (
		<View>
			<Text>Error</Text>
		</View>
	);
}

const styles = StyleSheet.create({
	container: {},
});

export default ErrorLoadingIndicator;
